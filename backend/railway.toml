# Railway deployment configuration for LENA FastAPI backend

[build]
# Use Docker build
builder = "dockerfile"

[deploy]
# Restart policy
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5

# Health check configuration
healthchecks.web.path = "/api/health"

# No startCommand needed - Dockerfile CMD handles PORT expansion via shell form
